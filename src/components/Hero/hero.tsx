import React from "react";
import { styled } from "styled-components";

const StyledHeroSection = styled.section``;
const Hero = (): JSX.Element => {
  return <StyledHeroSection>MainSection</StyledHeroSection>;
};

export default Hero;
